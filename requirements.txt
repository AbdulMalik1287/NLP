# Cross-Domain Complaint Understanding — full pipeline (GPU node)
# Python 3.11 recommended. Install torch FIRST from the CUDA index, see docs/ENVIRONMENT.md.

# --- core DL ---
torch==2.4.1                  # install via: pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cu121
transformers==4.44.2          # XLM-R, mBERT, BERT, mT5
tokenizers==0.19.1
sentencepiece==0.2.0          # required by XLM-R / mT5 tokenizers
datasets==2.21.0
accelerate==0.34.2
evaluate==0.4.3
safetensors==0.4.5

# --- classical baselines + metrics ---
scikit-learn==1.5.2           # TF-IDF, LogisticRegression, SVM, macro-F1, confusion matrices
scipy==1.14.1
numpy==1.26.4
pandas==2.2.2

# --- embeddings + retrieval ---
sentence-transformers==3.1.1  # contrastive losses (MultipleNegativesRanking), pooling utils
faiss-cpu==1.8.0.post1        # project-scale index fits CPU; swap for faiss-gpu only if needed

# --- summarization eval ---
rouge-score==0.1.2            # ROUGE-1/2/L
bert-score==0.3.13            # BERTScore
sacrebleu==2.4.3              # optional secondary metric
nltk==3.9.1

# --- language identification ---
fasttext-wheel==0.9.2         # lid.176 model, see docs/ENVIRONMENT.md for the .bin download
langdetect==1.0.9             # cheap sanity-check baseline only

# --- explainability ---
shap==0.46.0                  # intent classifier only
matplotlib==3.9.2

# --- text cleaning / dedup / transliteration ---
ftfy==6.2.3
regex==2024.9.11
datasketch==1.6.5             # MinHash near-duplicate detection for leakage prevention
rapidfuzz==3.9.7
indic-transliteration==2.3.61 # Devanagari <-> Latin, for Hinglish handling

# --- serving ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
streamlit==1.38.0             # prototype UI

# --- experiment tracking / config / reproducibility ---
hydra-core==1.3.2
omegaconf==2.3.0
mlflow==2.16.2                # or wandb; pick one before Week 4
tqdm==4.66.5
python-dotenv==1.0.1

# --- notebooks ---
jupyterlab==4.2.5
seaborn==0.13.2

# --- optional: persistent app storage (only if the app needs it) ---
# sqlalchemy==2.0.35
# psycopg[binary]==3.2.2
